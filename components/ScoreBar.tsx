// TODO: Build a reusable progress bar component (checkpoint-4)
// - Props: label, score, maxScore=100
// - Color-coded: green (70+), yellow (50-69), red (below 50)
// - Animate width on mount

export default function ScoreBar() {
  return null;
}
